import { cn } from '@/utilities/ui'
import React from 'react'
import ServerRichText from '@/components/RichText/server'

import type { ScrollingGalleryBlock as ScrollingGalleryProps } from '@/payload-types'

import { CMSLink } from '../../components/Link'

export const ScrollingGallery: React.FC<ScrollingGalleryProps> = (props) => {
  const { images } = props


  return (
    <div className="container my-16">
      
    </div>
  )
}
